---
phase: 1
plan: 2
wave: 1
---

# Plan 1.2: Client Utilities (Groq & Proxy Client)

## Objective
Implement core client utilities: a robust HTTP request client supporting user-agent rotation and optional Bright Data proxy routing, and a rate-limited Groq API client targeting `llama-3.3-70b-versatile` for structured AI processing.

## Context
- [.gsd/SPEC.md](file:///c:/Users/moham/Documents/GSD/agent-hub/.gsd/SPEC.md)
- [.gsd/phases/1/RESEARCH.md](file:///c:/Users/moham/Documents/GSD/agent-hub/.gsd/phases/1/RESEARCH.md)
- [src/config.py](file:///c:/Users/moham/Documents/GSD/agent-hub/src/config.py)

## Tasks

<task type="auto">
  <name>Implement HTTP Client with Proxy Support</name>
  <files>
    - [src/clients/http_client.py](file:///c:/Users/moham/Documents/GSD/agent-hub/src/clients/http_client.py)
  </files>
  <action>
    Create src/clients/http_client.py containing:
    - User-agent rotation list.
    - A request function that checks if `BRIGHTDATA_PROXY` is configured in `src/config.py`.
    - If configured, routes traffic (requests/urllib) through the proxy.
    - Error handling for timeouts and status codes.
  </action>
  <verify>
    Create a test script `tests/test_http_client.py` and run it:
    PowerShell: `python -m unittest tests/test_http_client.py`
  </verify>
  <done>HTTP client runs successfully, rotations work, and optional proxy parameters are correctly bound.</done>
</task>

<task type="auto">
  <name>Implement Groq API Client with Rate Limiting</name>
  <files>
    - [src/clients/groq_client.py](file:///c:/Users/moham/Documents/GSD/agent-hub/src/clients/groq_client.py)
  </files>
  <action>
    Create src/clients/groq_client.py using requests or the official `groq` SDK:
    - Load GROQ_API_KEY.
    - Set target model to `llama-3.3-70b-versatile`.
    - Implement basic exponential backoff / retry logic to respect Groq's 30 RPM limit.
    - Return structured JSON response parsed from LLM output.
  </action>
  <verify>
    Create a test script `tests/test_groq_client.py` and run it:
    PowerShell: `python -m unittest tests/test_groq_client.py`
  </verify>
  <done>Groq client correctly sends messages, handles errors, and returns structured outputs.</done>
</task>

## Success Criteria
- [ ] HTTP client successfully fetches webpages with rotating user-agents.
- [ ] Groq client authenticates, posts requests to Llama 3.3, and handles 429 retries.
- [ ] Unit tests pass for both client packages.
